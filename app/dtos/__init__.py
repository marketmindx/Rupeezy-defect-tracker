"""Data transfer objects.

Populated from Phase 5 onward: request/response dataclasses for the defect
module and the automation REST API, so route payload shapes are explicit
and decoupled from ORM models.
"""
